<%= hidden_field_tag :direction, params[:direction] %>
<%= hidden_field_tag :sort, params[:sort] %>

<table class="table table-striped table-bordered table-condensed sortable">
    <thead>
        <tr>
            <th style="width: 200px;">Code</th>
            <th><%= sortable "name", "Name"%></th>
            <th style="width: 40px;"></th>
        </tr>
    </thead>
    <tbody>
        <% @countries.each do |country| %>
        <tr>
            <td valign="top"> 
                <%= link_to country.code, edit_country_path(country), :remote => true %> 
            </td>
            <td valign="top"> 
                <%= country.name %> 
            </td>
            <td valign="top"> 
                <%= link_to edit_country_path(country), :remote => true, :style=>'height: 14px;', :title=>'Edit' do%> 
                    <i class="icon-edit"></i> 
                <% end %> 
                
                <a style="height: 14px; padding-left: 5px;" title="Delete"
                    data-backdrop="static" data-keyboard="true"
                    href="javascript:;"
                    onclick="show_confirm_modal('<%= country_path(country) %>',
                    'Are you sure you want to delete the country : <%= country.name%>');"> 
                    <i class="icon-remove"></i> 
                </a>
             </td>
        </tr>
        <% end %>
    </tbody>
    <tfoot>
        <tr>
            <td colspan="3"> <%= will_paginate @customers%> </td>
        </tr>
    </tfoot>
</table>
